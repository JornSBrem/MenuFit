# Shared Boundary

`src/shared` contains cross-module contracts and shared logic:

- config definitions
- contract types and validators
- deterministic utility logic used by multiple modules

Keep shared code framework-agnostic where possible.
